export function App() {
  return (
    <main className="app-shell">
      <section className="empty-state" aria-labelledby="app-title">
        <p className="eyebrow">Markdown-first slide studio</p>
        <h1 id="app-title">Write a deck. See it take shape.</h1>
      </section>
    </main>
  );
}
